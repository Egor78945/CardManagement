databaseChangeLog:
  - changeSet:
      id: v1-1
      author: test
      preConditions:
        - onFail: MARK_RAN
        - not:
            tableExists:
              tableName: ${TABLE_ROLE_TYPE_NAME}
      changes:
        - createTable:
            tableName: ${TABLE_ROLE_TYPE_NAME}
            columns:
              - column:
                  name: id
                  type: bigint
                  constraints:
                    primaryKey: true
                    nullable: false
                    unique: true
                  autoIncrement: true
              - column:
                  name: role
                  type: varchar(30)
                  constraints:
                    nullable: false
                    unique: true