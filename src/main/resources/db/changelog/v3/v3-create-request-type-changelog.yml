databaseChangeLog:
  - changeSet:
      id: v3-1
      author: test
      preConditions:
        - onFail: MARK_RAN
        - not:
            tableExists:
              tableName: ${TABLE_REQUEST_TYPE}
      changes:
        - createTable:
            tableName: ${TABLE_REQUEST_TYPE}
            columns:
              - column:
                  name: id
                  type: bigint
                  constraints:
                    primaryKey: true
                    nullable: false
                    unique: true
                  autoIncrement: true
              - column:
                  name: type
                  type: varchar(30)
                  constraints:
                    nullable: false
                    unique: true